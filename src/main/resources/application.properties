spring.application.name=AgriConnect
server.port=${PORT:8081}

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/agriconnect?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata&zeroDateTimeBehavior=CONVERT_TO_NULL}
spring.datasource.username=${DB_USERNAME:4en46bS74hUEojt.root}
spring.datasource.password=${DB_PASSWORD:lo5lfGLipCCq58e0}

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Python AI Microservice
ai.service.url=${AI_SERVICE_URL:http://localhost:5000}
ai.confidence.threshold=0.50

# OpenWeatherMap API
weather.api.key=${2844c1dc059d5dd5bfdf58bc532ef05e:}
weather.api.base-url=https://api.openweathermap.org/data/2.5/weather

# File Upload Limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Web static resource caching
spring.web.resources.cache.period=0


spring.application.name=AgriConnect
server.port=8081

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/agriconnect?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata&zeroDateTimeBehavior=CONVERT_TO_NULL
spring.datasource.username=root
spring.datasource.password=dbmsbase@786

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Python AI Microservice URL (YOLO11n Rice Leaf Disease Inference)
ai.service.url=http://localhost:5000
ai.confidence.threshold=0.50

# OpenWeatherMap API Configuration
weather.api.key=2844c1dc059d5dd5bfdf58bc532ef05e
weather.api.base-url=https://api.openweathermap.org/data/2.5/weather

# File Upload Limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Web static resource caching
spring.web.resources.cache.period=0
